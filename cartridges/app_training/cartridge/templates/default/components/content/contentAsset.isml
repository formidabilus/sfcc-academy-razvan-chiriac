<isdecorate template="common/layout/page">
    <!-- dwMarker="content" dwContentID="${pdict.content.UUID}" -->
    <isprint value="${pdict.content.body}" encoding="off" />
    <!-- END_dwmarker -->
    <isinclude url="${URLUtils.url('Page-Include', 'cid', pdict.pid)}" />
</isdecorate>
